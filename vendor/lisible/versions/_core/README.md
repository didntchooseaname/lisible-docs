# Lisible — reference core

This is the sober reference implementation used to validate shared behavior. It is not one of the six selectable visual variants.

All setup, configuration, authoring and validation instructions live in the [main README](../../README.md). Articles, global configuration, feature flags, routes and common assets come from `shared/`; this folder keeps only its Astro adapter and visual implementation.
