# Lisible — organique

This variant provides the interactive constellation, organic shapes and floating dock.

All setup, configuration, authoring and validation instructions live in the [main README](../../README.md). Articles, global configuration, feature flags, routes and common assets come from `shared/`; this folder keeps only its Astro adapter, theme components, styles and bilingual theme copy.
