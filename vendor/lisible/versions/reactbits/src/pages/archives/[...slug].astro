---
import ArchivesPage from "@/components/pages/ArchivesPage.astro";
import { FEATURES } from "@/site.config";

export function getStaticPaths() {
  return FEATURES.archives ? [{ params: { slug: undefined } }] : [];
}
---

<ArchivesPage locale="fr" />
